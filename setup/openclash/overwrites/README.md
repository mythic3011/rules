# OpenClash overwrites

No overwrite repository is vendored here. The previous Git submodule was removed because an empty or stale submodule makes first-time setup harder and hides an external dependency behind repository state.

If an overwrite is required, install it explicitly from its upstream project and pin the version you intend to use. Normal consumption of this repository's published profiles does not require an overwrite submodule.
