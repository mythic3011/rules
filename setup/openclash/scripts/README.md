# OpenClash operational scripts

`../install.sh` is the supported consumer entrypoint.

Files in this directory are advanced operational helpers used by specific maintenance flows (AdGuard refresh, fail-closed routing checks, OpenClash dev installation, and controller dry-run behavior). They are not required for normal profile consumption.
