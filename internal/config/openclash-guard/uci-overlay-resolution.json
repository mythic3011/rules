{
  "schemaVersion": 1,
  "purpose": "Machine-readable contract for UCI overlay authority resolution (Layer B). Implemented by shell/apps/openclash-guard/uci-overlay-resolve.sh and asserted by tests/test_openclash_guard_uci_overlay_resolve.py. Scope discipline: compute effective values ONLY where an authoritative source defines the gate; everything else is an explicit contract gap (deferred), never invented.",
  "trustModel": {
    "policy": "UCI is operator intent, never a second policy authority. Computed effective values constrain the operator request by signed policy (ceiling/floor) and live capability; UCI can only NARROW, never widen.",
    "rawVsEffective": "The snapshot keeps operator intent (raw) distinct from the resolved value (effective). Diagnostics report both so drift is explainable.",
    "resolveGate": "guard_uci_overlay_resolve() REQUIRES a loaded AND valid Layer-A snapshot. On an invalid or unloaded snapshot it returns non-zero and presents NO effective state as usable.",
    "authorityInputs": "Resolution consumes validated UCI intent + an AUTHENTICATED signed policy + an OBSERVED live capability. All required authority inputs must be valid before _GUARD_UCO_RESOLVED=1. A missing/malformed/unvalidated signed policy MUST NOT degrade into permissive defaults (e.g. an empty services list would hide the fail-closed floor); the resolver validates the policy surface it consumes (services + protectionClasses + class-field consistency: every service references an existing class; directAllowed and firewallKillSwitch are boolean). The live DNS observation must be a real observed value (adguardhome|dnsmasq|none); an empty/unset/unexpected value means the observation is unavailable or not performed — NOT the observed value 'none'. Any unavailable authority input makes the whole normal resolution refuse (rc 3, resolved state stays 0, gated effective values unavailable). At production wiring time the resolver is expected to consume state already validated by guard_policy_load(); the focused check here is over the resolver-consumed policy surface so the trust contract is identical. Diagnostics reports authorityInputs availability without mutating resolved state.",
    "snapshotCoherence": "Layer-A collection is snapshot-coherent, not atomic. The full `uci show` catalog is captured ONCE and both option paths and per-option values are derived from that single captured representation (no second `uci show` just for path enumeration, no per-option `uci get` re-reads that would mix generations). Because capture and validation are not a true atomic op, the catalog is re-captured afterward and compared; if the package changed mid-read the candidate is discarded (nothing becomes observable) and the read is retried (bounded by GUARD_UCI_OVERLAY_MAX_ATTEMPTS, default 2). A persistent generation change yields a non-zero load and 'snapshot not coherent' rather than publishing a mixed-generation snapshot. Residual guarantee: before/after change detection with discard-on-drift — NOT a guarantee that the captured window itself was atomic.",
    "resolutionCommitAtomicity": "The internal commit is atomic. guard_uci_overlay_invalidate_resolved_state() runs first; only if _guard_uci_overlay_resolve_apply() succeeds is _GUARD_UCO_RESOLVED set to 1, otherwise state is invalidated again and resolution returns non-zero. A partial computation can never leave partial effective globals behind or accidentally mark the state resolved. This uses explicit handling, not set -e reliance inside shell functions.",
    "resolvedStateLifecycle": "Layer-B maintains an explicit resolved-state flag (_GUARD_UCO_RESOLVED, default 0). Every overlay re-load invalidates all resolved state (_GUARD_UCO_EFFECTIVE_* and notes cleared, flag -> 0) BEFORE a new snapshot is observable, so a stale effective value can never leak across snapshots. The flag becomes 1 only after the complete normal resolution path succeeds. guard_uci_overlay_resolve_state_valid() means loaded AND layer-A-valid AND layer-B-resolution-completed (NOT merely loaded+valid), and goes false again after any re-load until re-resolved. guard_uci_overlay_effective() for a resolved gated option REFUSES (non-zero) when no completed resolution exists for the current snapshot; it never falls back to the normalized UCI value. guard_uci_overlay_resolve_diagnostics() is read-only: inference runs in a subshell so it cannot mutate _GUARD_UCO_RESOLVED, _GUARD_UCO_EFFECTIVE_*, or notes in the caller."
  },
  "computed": {
    "signed-policy-gated-route-mode": {
      "applies": ["routing.chatgpt", "routing.claude", "routing.grok"],
      "rule": "A requested 'direct' is honoured only when the service's protection class has directAllowed=true; otherwise effective falls back to 'proxy'. Region constraints are NOT part of this config-time gate (allowedRegions constrains live route evaluation in guard_policy_region_allowed, not the snapshot)."
    },
    "signed-policy-floor": {
      "applies": ["dns.fail_closed"],
      "rule": "If any service protection class has firewallKillSwitch=true OR directAllowed=false (mirrors guard_policy_needs_failclosed), fail-closed is a signed REQUIREMENT: an operator dns.fail_closed=0 cannot lower the floor and effective stays 1. Otherwise the operator value is honoured."
    },
    "live-capability-gated-backend": {
      "applies": ["dns.backend"],
      "rule": "Mirrors guard_dns_backend() detection (adguardhome | dnsmasq | none). 'auto' resolves to the detected backend else 'none'; an explicit request is honoured only when it equals the detected backend, else effective is 'none' (a preference cannot install capability). Downstream consumers key off this EFFECTIVE backend, not the raw live input."
    }
  },
  "gaps": {
    "note": "These options are contract-marked (signed-policy-gated / live-capability-gated) but their exact Layer-B resolution semantics are NOT defined by any authoritative contract today. They are surfaced as DEFERRED (normalized passthrough, explicitly flagged) and MUST NOT be consumed as an authoritative effective value until a contract update lands. Unresolved semantics are intentionally preferred over invented semantics.",
    "deferred": [
      {
        "option": "dns.resolver_sync",
        "reason": "No authoritative contract defines resolver-sync capability. Current runtime behavior is NOT a simple 'backend is capable' flag: guard_dns_domain_set_backend maps dnsmasq->dnsmasq-nftset, and adguardhome only promotes via the separate guard_resolver_sync_backend capability verification. Treating adguardhome/dnsmasq as generically sync-capable would invent semantics."
      },
      {
        "option": "routing.direct_region",
        "reason": "#122 marks it signed-policy-gated but no contract defines the config-time gate (allowedRegions gates live route evaluation, not this snapshot). Defer until defined."
      },
      {
        "option": "routing.proxy_region",
        "reason": "Same as routing.direct_region: gate undefined; only the primaryOrder VALIDATION (Layer A) is defined, not a signed-policy resolution."
      },
      {
        "option": "udp.enabled",
        "reason": "#122 marks it signed-policy-gated but no authoritative gate ties it to a signed policy field. Defer."
      },
      {
        "option": "udp.src_ip",
        "reason": "#122 marks it signed-policy-gated but no authoritative gate ties the source-IP list to a signed policy field. Defer."
      }
    ]
  },
  "redaction": "Resolution reasons name the constraint that fired (e.g. 'direct not permitted by signed policy', 'requested DNS backend not detected live') and never echo URLs, tokens, or credentials."
}
