${header}

# Static/global settings remain owned by the legacy Python relaxed profile.
${static-top-level}

proxy-providers:
${proxy-providers}

dns:
${dns}

proxy-groups:
${proxy-groups}

rules:
${rules}

rule-providers:
${rule-providers}
